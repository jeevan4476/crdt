[package]
name = "crdt"
version = "0.1.0"
edition = "2024"

[dependencies]
bincode = "1.3.3"
serde = { version = "1.0.228", features = ["derive"] }
wincode = { version = "0.5.2", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.149"
